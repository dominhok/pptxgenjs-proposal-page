interface:
  display_name: "PPT 시각 교정"
  short_description: "렌더링과 OfficeCLI로 PPT 레이아웃을 반복 교정합니다"
  default_prompt: "Use $pptx-visual-proofreader to preserve this presentation’s design while fixing overflow, overlap, spacing, and readability problems."
